import Model, { attr } from '@ember-data/model';

export default class RoomModel extends Model {

    @attr img;
    @attr name;
    @attr price;
    @attr distance;
    @attr date;
    @attr img1;
    
}